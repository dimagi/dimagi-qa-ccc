[pytest]
; No -s here: output capture must stay on so executed steps appear per-test
; in the pytest-html report. Pass -s manually when debugging locally.
addopts = -v --headed
testpaths = tests
pythonpath = .
